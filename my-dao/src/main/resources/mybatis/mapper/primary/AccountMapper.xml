<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.rex.my.dao.mapper.primary.AccountMapper">
    <resultMap id="BaseResultMap" type="com.rex.my.model.dao.primary.Account">
        <!--
          WARNING - @mbg.generated
          This element is automatically generated by MyBatis Generator, do not modify.
        -->
        <id column="id" jdbcType="VARCHAR" property="id"/>
        <result column="name" jdbcType="VARCHAR" property="name"/>
        <result column="money" jdbcType="DECIMAL" property="money"/>
        <result column="removed" jdbcType="BIT" property="removed"/>
        <result column="create_date" jdbcType="TIMESTAMP" property="createDate"/>
        <result column="update_date" jdbcType="TIMESTAMP" property="updateDate"/>
        <result column="user_id" jdbcType="VARCHAR" property="userId"/>
        <result column="account_type_id" jdbcType="VARCHAR" property="accountTypeId"/>
    </resultMap>

    <insert id="insertSelective" parameterType="com.rex.my.model.dao.primary.Account">
        <selectKey keyProperty="id" resultType="string" order="BEFORE">
            select replace(uuid(), '-', '') as id from dual
        </selectKey>
        insert into account
        <trim prefix="(" suffix=")" suffixOverrides=",">
            id, name, money,
            <if test="removed != null">removed,</if>
            create_date,
            <if test="updateDate != null">update_date,</if>
            user_id, account_type_id,
        </trim>
        values
        <trim prefix="(" suffix=")" suffixOverrides=",">
            #{id}, #{name}, #{money},
            <if test="removed != null">#{removed},</if>
            #{createDate},
            <if test="updateDate != null">#{updateDate},</if>
            #{userId}, #{accountTypeId}
        </trim>
    </insert>

    <select id="selectAll" resultMap="BaseResultMap">
        select * from account where removed = 0
    </select>

    <select id="selectByPrimaryKey" parameterType="string" resultMap="BaseResultMap">
        select * from account where id = #{id} and removed = 0
    </select>

    <update id="update2DeleteByPrimaryKey" parameterType="string">
        update account set  removed = 1 where id = #{id} and removed = 0
    </update>

    <update id="updateSelectiveByPrimaryKey" parameterType="com.rex.my.model.dao.primary.Account">
        update account
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="money != null">money = #{money},</if>
            <if test="removed != null">removed = #{removed},</if>
            <if test="accountTypeId != null">account_type_id = #{accountTypeId},</if>
            update_date = #{updateDate}
        </set>
        <where>
            id = #{id} and removed = 0
        </where>
    </update>
</mapper>