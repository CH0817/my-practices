<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.rex.my.dao.mapper.mysql.UserMapper">
    <resultMap id="BaseResultMap" type="com.rex.my.entity.mysql.User">
        <id column="id" jdbcType="INTEGER" property="id"/>
        <result column="email" jdbcType="VARCHAR" property="email"/>
    </resultMap>
    <delete id="deleteByPrimaryKey" parameterType="java.lang.Integer">
        delete
        from user
        where id = #{id,jdbcType=INTEGER}
    </delete>
    <insert id="insert" parameterType="com.rex.my.entity.mysql.User">
        insert into user (id, email)
        values (#{id,jdbcType=INTEGER}, #{email,jdbcType=VARCHAR})
    </insert>
    <update id="updateByPrimaryKey" parameterType="com.rex.my.entity.mysql.User">
        update user
        set email = #{email,jdbcType=VARCHAR}
        where id = #{id,jdbcType=INTEGER}
    </update>
    <select id="selectByPrimaryKey" parameterType="java.lang.Integer" resultMap="BaseResultMap">
        select id, email
        from user
        where id = #{id,jdbcType=INTEGER}
    </select>
    <select id="selectAll" resultMap="BaseResultMap">
        select id, email
        from user
    </select>
</mapper>